import React from 'react';
import {
    AppBar,
    Toolbar,
    Typography,
    Avatar,
    IconButton,
} from '@mui/material';

export interface BaseProps {
}

const BotlandiaBase: React.FC<BaseProps> = (props: BaseProps) => {
    return (
        <></>
    );
};

export default BotlandiaBase;
